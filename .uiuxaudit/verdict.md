# /uiuxaudit — DESIGN FORENSIC REPORT (scoped)

**URL:** https://agentik-design-1ejn9b8aq-agentik-oss-projects.vercel.app/
**Scope:** AgentikDesign post-deploy: home page renders, no console-driven layout shifts, sign-in modal opens cleanly
**Date:** 2026-05-07T13:38Z
**Score:** **61 / 100 — Grade C**

---

## Scope verdict

| Ask | Status | Detail |
|---|---|---|
| Home page renders | **PASS w/ issues** | HTTP 200, content visible after CSR hydration. But title/loading-shell/welcome-modal still say "Open Design" — rebrand incomplete. |
| No console-driven layout shifts | **PASS for CLS, FAIL for console** | Measured CLS = 0 via PerformanceObserver (zero shifts). Console still has 2× 401 errors on `/api/templates` (silencing fix from commit 3672f4a is incomplete) and 2× Clerk dev-key warnings. |
| Sign-in modal opens cleanly | **PARTIAL** | Modal opens cleanly *after* dismissing the welcome modal (correctly says "Sign in to Agentik Design"). On a fresh load the welcome modal **blocks** the Sign in button — Playwright click TIMED OUT until welcome was skipped. Reproducible. |

---

## Top 8 findings

| ID | Sev | Title | Where |
|---|---|---|---|
| **F1** | 🔴 CRITICAL | Brand identity split: app surfaces say "Open Design", Clerk modal says "Agentik Design" | `apps/web/app/layout.tsx:10`, `apps/web/app/(app)/[[...slug]]/client-app.tsx:12`, `apps/web/src/i18n/locales/*.ts` (~12 locales) |
| **F2** | 🟠 HIGH | Welcome modal auto-popups for signed-out visitors and blocks the Sign in button | welcome-modal trigger logic in `src/components/SettingsDialog.tsx` or root view |
| **F3** | 🟠 HIGH | Production deployment uses Clerk `pk_test_*` development key — "Development mode" badge visible inside sign-in modal | Vercel env var `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` |
| **F4** | 🟡 MED | Recent silencing of auth-gate fetches (commit 3672f4a) is incomplete — `/api/templates` still 401s in console | `apps/web/app/api/templates/route.ts` + caller |
| **F5** | 🟡 MED | `<button component="SignInButton">` invalid HTML attribute leaks into DOM | `apps/web/app/layout.tsx:60-62` |
| **F6** | 🟡 MED | Welcome modal overflows at 375 px (CLI/BYOK tabs and "Test ↻ Rescan" clip beyond viewport) | welcome modal CSS / shell |
| **F7** | 🟢 LOW | Sign in button has no visible hover/focus state | `apps/web/app/layout.tsx:48-72` |
| **F8** | 🟢 LOW | Footer chip "Follow @nexudotio" — upstream brand leftover | grep `nexudotio` apps/web |

---

## Phase scores (weighted, /380 applicable)

| Phase | Score /10 | Notes |
|---|---|---|
| P1 Colors | 7 | Beige/orange accent system coherent |
| P2 Typography | 8 | Clean scale, single font, hierarchy intact |
| P3 Spacing | 7 | Consistent at desktop, clipping at 375 px |
| P4 Components | 5 | SignInButton DOM attr leak, no hover/focus |
| **P5 Coherence** | **3** | **Brand split — app vs Clerk** |
| P6 Interaction | 4 | Welcome modal blocks Sign in |
| P7 Responsive | 4 | Mobile overflow on welcome modal |
| P8 Accessibility | 6 | Loading-shell SR leakage, no focus ring |
| P9 Smells | 4 | Dev keys in prod, 401 console, legacy nexudotio |
| P10 Hierarchy | 5 | Two competing primaries (Get started vs Sign in) |
| P11 Microcopy | 4 | "Open Design" string × 4 surfaces |
| P12 Performance | 7 | CLS=0, FCP 764ms, but full-CSR bailout |
| P13 Dark mode | 7 | localStorage-driven, OK |
| P14 System maturity | 7 | Inherited solid foundation, brand tokens incomplete |
| P15 Navigation | 7 | Out of scope, light pass |
| P16 Onboarding | 4 | Welcome modal fires for signed-OUT users (wrong audience) |
| P17 Data viz | N/A | — |
| P18 Error recovery | 5 | 401s reach console, no UI recovery |
| **P19 Brand** | **2** | **Open Design everywhere except Clerk** |

---

## Evidence (screenshots in `.uiuxaudit/screenshots/before/`)

- `home-1440-loaded.png` — desktop initial paint with welcome modal blocking
- `home-1440-after-dismiss.png` — desktop after Skip-for-now
- `home-375.png` — mobile shows modal overflow + Sign in button clipped
- `sign-in-modal-attempt.png` — Clerk modal correctly branded "Sign in to Agentik Design" with "Development mode" badge
- `console.json` — 4 messages (2 errors / 2 warnings)
- `network-failures.json` — 2× 401 on /api/templates
- `dom-snapshot.json` — full DOM snapshot

---

## Recommended next moves (NOT executed in this run)

1. **F1 (rebrand surfaces)** — single mass-replace pass on:
   - `apps/web/app/layout.tsx` line 10 → `title: 'Agentik Design'`
   - `apps/web/app/(app)/[[...slug]]/client-app.tsx` line 12 → `Loading Agentik Design…`
   - `apps/web/src/i18n/locales/*.ts` → `'app.brand': 'Agentik Design'` across all locales
   - Welcome modal heading copy
   - `apps/web/src/runtime/exports.ts` → "Generated by Agentik Design"
2. **F2 (welcome blocks sign-in)** — gate the welcome modal behind `<SignedIn>` or trigger only after first authenticated session.
3. **F3 (dev key in prod)** — provision a Clerk production instance and swap `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` in Vercel env.
4. **F4 (templates silencing)** — wrap the templates fetcher behind a `useAuth().isSignedIn` check, or short-circuit at the API route.
5. **F5/F7 (button)** — replace inline-styled wrapper with a shadcn `Button` variant; pass children through the `SignInButton` cleanly.
6. **F6 (mobile overflow)** — set modal `max-width: 92vw` and allow internal `overflow-x: auto` on the tab strip.
7. **F8 (legacy social link)** — point to Agentik OS social or remove.

---

*Compliance: preamble v1.0 emitted, score normalized, scope-narrow audit honored full phase depth (no streamlined variant).*
